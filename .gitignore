# Secrets & credentials — NEVER commit these
# .env: the not-yet-rewritten parts of the app still read it during the v2
# rewrite; config.json: the new settings file for already-rewritten parts.
.env
config.json

# Data
data/
*.db

# Python
.venv/
__pycache__/
*.pyc
.uv/

# Tooling caches (uv run nox)
.nox/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Node / frontend
node_modules/
frontend/dist/
frontend/node_modules/

# OS / editor
.DS_Store
.vscode/
.idea/

# Personal brainstorm inbox (filed into OnePager, then cleared)
todos.md
